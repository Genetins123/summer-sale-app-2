const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const sales = await prisma.sale.findMany({ include: { _count: { select: { items: true } } } }); console.log(JSON.stringify(sales, null, 2)); const items = await prisma.saleItem.groupBy({ by: ['saleId'], _count: { id: true, appliedAt: true, restoredAt: true } }); console.log(JSON.stringify(items, null, 2)); } run().finally(() => prisma.$disconnect());
